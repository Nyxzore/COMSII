select REPLACE(coach_name, 'Coach', '') as actual_coach_name, sport_name, count(result) as total_wins from participation p
join event e on p.event_id = e.event_id
join sport s on s.sport_id = e.sport_id
where result = 'Win'
group by (coach_name,sport_name)
order by total_wins desc;